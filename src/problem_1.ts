// // loop the name
// const loopTheName = (name: string, count: number = 3): void => {
//   for (let i = 0; i < count; i++) {
//     console.log(`The name ${name} is looped for ${i + 1} times`);
//   }
// };

// loopTheName("John", 10);
